apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: elasticsearch-tls
  namespace: elastic
spec:
  secretName: tls-elasticsearch
  dnsNames:
    - elastic.wgbh-mla.org
  issuerRef:
    name: letsencrypt-production
    kind: ClusterIssuer
